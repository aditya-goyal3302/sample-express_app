<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" type="text/css" href="/CSS/add-user.css">
</head>
<body>
    <h1>Welcome to portal</h1>
    <a href="/">Home</a>
    <a href="/admin/users">View Users</a><br><br>
    <form action="/admin/<%= editing == true ?'edituser':'adduser'%>" method="POST">
        <input type="hidden" name="id" value="<%= editing == true ?user._id:''%>">
        <div>
            <label for="name">Name:</label>
            <input type="text" id="name" name="name" required value="<%= editing == true ?user.name:''%>">
        </div>
        <div>
            <label for="city">City:</label>
            <input type="text" id="city" name="city" required value="<%= editing == true ?user.city:''%>">
        </div>
        <div>
            <label for="email">Email:</label>
            <input type="email" id="email" name="email" required value="<%= editing == true ?user.email:''%>">
        </div>
        <div>
            <label for="age">Age:</label>
            <input type="number" id="age" name="age" required value="<%= editing == true ?user.age:''%>">
        </div>
        <button type="submit">Add User</button>
    </form>
    
</body>
</html>